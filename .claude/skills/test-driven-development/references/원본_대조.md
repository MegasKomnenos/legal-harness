# 원본 대조: superpowers:test-driven-development

- 원본: claude-plugins-official/superpowers/6.1.0, 경로 `~/.claude/plugins/cache/claude-plugins-official/superpowers/6.1.0/skills/test-driven-development/`
- 원본 SKILL.md 해시: 60d2609ca56c7177a98f462fb71af77844ce92af (371줄)
- 동반 리소스: testing-anti-patterns.md 해시 e77ab6b6d65ecfb0d8212a32186bf68d18b5eade (299줄, 지시 텍스트로 전문 통독)
- 대조 일자: 2026-07-02
- 대조한 로컬 정본: `.claude/settings.json`(hook 5종·PreCompact·permissions), `.claude/settings.local.json`, `.claude/CLAUDE.md`, legal-harness·memory-harness·adapt-skill·casenote-fetch·extract-documents SKILL.md, `기억/색인.md`, `기억/세칙.md`(v14), `기억/layer3/L-0001.md`(검증 인프라), `기억/layer3/L-0005.md`(AI 편향·방법론), `harness/quality/검증_체크리스트.md`
- 보류 목록: harness/style 3종·관리_절차(4절은 T-11 검토 중 문언 확인)·디렉토리_지도·data 사전 3종·PDF_서식, L-0002~0004·L-0006~0008 (TDD 주제와 무접촉 또는 간접 접촉 판정)
- 로컬 코드 실태 실측: 테스트 스위트·러너 전무(python 스크립트 9종: harness/scripts 7 + extract_all.py + fetch_casenote.py; legal-workbench는 express 서버, npm test 미정의). 코드 검증 관례는 실데이터 실행·회귀 확인(M-0026)·self-test(validate_harness.py). 주 산출물은 법률 문서(산문)이며 그 검증 정본은 legal-harness 검증 루프(Phase 1 hook 기계검증 + Phase 2 독립 서브에이전트).
- 독립 검증: 2026-07-02 수행. 발견 전부 수용(1건 부분 수용): T-14·15 축 재판정(D→A 혼재, 자기모순 위험), T-21 흡수 원리 2→4 확장("8할" 근거 철회), T-18 누락 2항목 승계, T-03·05·07·12·23 근거 교정, T-11·24 self-test 트리거 문언 정밀화, T-02 반복 지시·T-13·20·21 의도 필드 보완, T-15 반복 구조 부분 존치. 반영 행에 [반증 반영] 표시.

## 대조표 (행별 목록 형식, 대조표_양식.md 스키마 준수)

### T-01
- 원본 위치: frontmatter description
- 지시 원문: "Use when implementing any feature or bugfix, before writing implementation code"
- 유형: 전제(트리거 규범)
- 원본 의도: 구현 코드를 쓰기 전 시점에 이 스킬이 로드되게 함(사후 적용은 무의미하므로)
- 대조 로컬 규범: 절차 정본 legal-harness("법률 문서 생성"의 전 과정 정본), advisory CLAUDE.md(법률 문서 작업 원칙)
- 판정 축: A(산출물 형태 전제) + C(관할 겹침 위험)
- 처리안: 대체
- 수단: 적용 대상을 "코드 산출 작업"(스크립트·서버 등)으로 명확화하고, 법률 문서·산문 산출물은 legal-harness 검증 루프 관할임을 description에서 선언. "원본 대신 이 커스텀본 사용" 우선 선언 포함(adapt-skill 생성 규칙 3요소)
- 근거: "any feature or bugfix"가 이 프로젝트에서는 문서 작업까지 포섭될 수 있어 정본 관할 침범. 시점 규범(구현 전 로드)의 의도는 보존

### T-02 [반증 반영: 반복 지시 명시]
- 원본 위치: Overview + Repeat 절
- 지시 원문: "Write the test first. Watch it fail. Write minimal code to pass." / "Core principle: If you didn't watch the test fail, you don't know if it tests the right thing." / (Repeat) "Next failing test for next feature."
- 유형: 명령(핵심 절차·사이클 반복) + 원리
- 원본 의도: 검증 장치 자체의 유효성을 "실패의 직접 관찰"로 증명. 통과만 본 검증은 아무것도 검증하지 않았을 수 있음. 이 사이클을 수정 단위마다 반복
- 대조 로컬 규범: 기계 강제층·절차 정본에 코드용 대응물 부재(테스트 러너 전무 실측). 원리 자체는 L-0005 표상 드리프트("exit 0을 정합의 증명으로 오인하지 않는다")와 동방향
- 판정 축: A(테스트 프레임워크 전제)
- 처리안: 대체
- 수단: "실패하는 재현 실행을 먼저 확보한다"로 재정식화: 수정 전 재현 입력(실패 사례)을 확정하고 실제 실행으로 실패를 관찰한 뒤 수정한다. 수정 단위마다 이 사이클을 반복한다. 로컬 실증: M-0026(extract_all.py 개선에서 벡터 PDF 재현 사례로 실패 확인 후 파라미터 수정, 정상 PDF 회귀 확인)
- 근거: 프레임워크는 없으나 "실패 관찰 선행"의 의도는 스크립트 직접 실행으로 달성 가능하고 이미 로컬 관례로 실증됨. Repeat 절의 반복 지시를 이 행에 명시 승계(독립 검증 3-3)

### T-03 [반증 반영: 근거 교정]
- 원본 위치: Overview
- 지시 원문: "Violating the letter of the rules is violating the spirit of the rules."
- 유형: 톤(합리화 차단 절대성)
- 원본 의도: "정신만 지키면 된다"는 합리화 경로를 문면 수준에서 봉쇄(Why Order Matters의 "it's spirit not ritual" 반박과 한 쌍)
- 대조 로컬 규범: advisory CLAUDE.md 작문 규칙은 "이 프로젝트에서 내가 쓰는 모든 산문(메타 문서 포함, 개방 열거)"에 적용되므로 커스텀 스킬 본문도 포섭한다. 절대성 문장 자체는 과장 표현이 아니라 규칙의 적용 강도 선언이므로 작문 규칙과 저촉하지 않음
- 판정 축: D(표층 언어만)
- 처리안: 대체(번역 수준)
- 수단: 절대성 강도를 보존한 한국어 문장으로 변환
- 근거: 절대성이 의도적 설계(합리화 봉쇄)이므로 완화는 의도 훼손. adapt-skill 축 D 처리 원칙의 "의도 강도 보존" 적용. (교정: 당초 "작문 규칙은 스킬 규범문 대상 아님"이라 썼으나 CLAUDE.md 적용 범위 문언과 어긋나 정정. 독립 검증 1-2)

### T-04
- 원본 위치: When to Use
- 지시 원문: "Always: New features / Bug fixes / Refactoring / Behavior changes"
- 유형: 전제(적용 범위)
- 원본 의도: 적용 예외를 좁혀 "이번만 스킵" 합리화를 차단
- 대조 로컬 규범: T-01과 동일(주 산출물이 산문인 프로젝트)
- 판정 축: A(조건화 필요)
- 처리안: 조건화
- 수단: "코드 산출 작업의" 신규 기능·버그 수정·리팩토링·동작 변경으로 한정. 목록은 열거형 예시로 개방 유지(CLAUDE.md 해석 원칙)
- 근거: 코드 작업 내에서는 원본 범위가 그대로 유효

### T-05 [반증 반영: 근거 교체]
- 원본 위치: When to Use Exceptions
- 지시 원문: "Exceptions (ask your human partner): Throwaway prototypes / Generated code / Configuration files" + "Thinking 'skip TDD just this once'? Stop. That's rationalization."
- 유형: 명령(예외 경로) + 톤
- 원본 의도: 예외는 존재하되 자기 판단이 아니라 사용자 승인 경로로만 열림
- 대조 로컬 규범: memory-harness 핵심층 5(핵심 변경은 사용자 제안·승인 경로만)와 동방향
- 판정 축: D(언어·페르소나)
- 처리안: 대체(변환)
- 수단: "your human partner" 표현을 "사용자"로, 예외 목록을 로컬 실태(일회성 스크래치 스크립트, 생성된 코드, 설정 파일 등)로 변환, 승인 경로 유지
- 근거: 예외의 승인 경로화는 로컬 문화와 정합, 표층만 변환. (교정: 당초 보류 선언한 L-0006을 색인 표상만으로 인용했던 근거를 전문 적재된 memory-harness 핵심층으로 교체. 독립 검증 1-3)

### T-06
- 원본 위치: The Iron Law
- 지시 원문: "NO PRODUCTION CODE WITHOUT A FAILING TEST FIRST" / "Write code before the test? Delete it. Start over." / "No exceptions: Don't keep it as 'reference' / Don't 'adapt' it while writing tests / Don't look at it / Delete means delete" / "Implement fresh from tests. Period."
- 유형: 명령·금지(최상위 규칙)
- 원본 의도: (1) 검증 없이 작성된 코드의 신뢰 불가, (2) "참조 보관"이 사후 테스트로 회귀하는 누수 경로 봉쇄, (3) 매몰비용 합리화 차단
- 대조 로컬 규범: 코드용 테스트 인프라 부재(실측). 산문 영역에서는 legal-harness 8단계(self-check 기계 한정)·9단계(독립 검증 실질 게이트)가 같은 의도(생성자 자기 통과 차단)를 담당. CLAUDE.md 불변 자산 보호(harness/ 수정은 사용자 확인 선행)
- 판정 축: A(전제) + C(산문 영역 관할)
- 처리안: 대체
- 수단: 코드 작업 한정으로 "실패하는 재현 실행 없이 수정 코드를 확정하지 않는다"로 재정식화. 삭제 규칙(재현 확보 전에 쓴 수정분은 버리고 재현부터)과 그 절대성 유지. 산문 산출물에의 적용은 명시적으로 배제하고 legal-harness 검증 루프로 위임(관할 지도)
- 근거: 의도 3요소는 코드 작업에서 전부 보존 가능. "Delete means delete"의 강도는 매몰비용 차단 설계이므로 유지

### T-07 [반증 반영: 근거 층위 정정]
- 원본 위치: RED - Write Failing Test
- 지시 원문: "Write one minimal test showing what should happen." + Requirements: "One behavior / Clear name / Real code (no mocks unless unavoidable)" + Good/Bad typescript 예시
- 유형: 명령 + 예시
- 원본 의도: 재현·검증 단위를 최소화해 실패 원인의 귀속을 명확히 하고, mock이 아닌 실물을 검증
- 대조 로컬 규범: 실데이터 검증 관례의 실제 소재는 기억층(M-0020·0023·0026의 실측 교차대조·실물 재현). extract-documents·casenote-fetch SKILL.md는 실경로 사용 예시를 보일 뿐 검증 관례의 성문 규범은 아님
- 판정 축: A(프레임워크·언어 예시)
- 처리안: 대체
- 수단: "하나의 동작을 보이는 최소 재현 사례(실제 입력 파일·실제 호출)를 확보한다"로 변환. 예시를 로컬 실증(M-0026: 벡터 PDF 1건을 재현 입력으로 삼음)으로 교체. "mock 불가피할 때만"은 "실물 데이터 우선, 실물로 불가한 부분만 대역"으로 승계
- 근거: 최소 단위·실물 우선의 의도는 로컬 관례(기억층 실증)가 이미 구현. (교정: 근거 층위를 스킬 정본에서 기억층으로 정정. 독립 검증 1-6)

### T-08
- 원본 위치: Verify RED
- 지시 원문: "MANDATORY. Never skip." + `npm test path/to/test.test.ts` + "Confirm: Test fails (not errors) / Failure message is expected / Fails because feature missing (not typos)"
- 유형: 명령(필수 게이트) + 도구 전제
- 원본 의도: 검증 장치가 "옳은 이유로" 실패함을 확인(오류로 인한 실패와 기능 부재로 인한 실패의 구별)
- 대조 로컬 규범: 도구 전제(npm) 로컬 부재. 실행 관례는 python 직접 실행(permissions.allow의 `python extract_all.py *` 등)
- 판정 축: A(도구)
- 처리안: 대체
- 수단: 재현 실행(python 스크립트 직접 호출)으로 실패를 관찰하고, 실패가 예상 이유(기능 부재·버그)인지 오류(오타·환경)인지 구별하는 확인 지시 유지. MANDATORY의 강도 보존
- 근거: 실패 이유의 구별은 도구 무관하게 유효한 의도

### T-09
- 원본 위치: Verify RED 말미
- 지시 원문: "Test passes? You're testing existing behavior. Fix test. / Test errors? Fix error, re-run until it fails correctly."
- 유형: 명령(분기 처리)
- 원본 의도: 재현 사례 자체의 결함을 수정 착수 전에 걸러냄
- 대조 로컬 규범: L-0005 표상 드리프트(진단·처방도 정본 대조 필요: "재현이 실은 재현이 아님"을 걸러내는 동방향)
- 판정 축: D(표층)
- 처리안: 대체(변환)
- 수단: 한국어 변환, 재현 사례가 이미 통과하면 "고칠 것이 재현되지 않은 것"이므로 재현부터 재구성
- 근거: 분기 논리는 도구 중립

### T-10
- 원본 위치: GREEN - Minimal Code
- 지시 원문: "Write simplest code to pass the test." + "Don't add features, refactor other code, or 'improve' beyond the test." + Good/Bad 예시(YAGNI)
- 유형: 명령·금지
- 원본 의도: 검증 범위 밖의 무근거 변경 차단(변경의 추적 가능성)
- 대조 로컬 규범: 저촉 규범 없음. M-0026(요청받은 "부실 감지"가 파괴적이라 기각하고 최소 파라미터 수정만 적용)이 같은 방향의 로컬 실증
- 판정 축: D(언어·예시만. 실질 F에 가까움)
- 처리안: 대체(변환)
- 수단: 한국어 변환 + 예시를 로컬 사례로 교체
- 근거: 최소주의 원칙 자체는 무충돌, 표층만 로컬화

### T-11 [반증 반영: pristine 조건화·self-test 문언 정밀화]
- 원본 위치: Verify GREEN
- 지시 원문: "MANDATORY." + npm test + "Confirm: Test passes / Other tests still pass / Output pristine (no errors, warnings)" + "Test fails? Fix code, not test. / Other tests fail? Fix now."
- 유형: 명령(필수 게이트)
- 원본 의도: 수정이 목표를 달성했고 다른 것을 깨지 않았음(회귀 부재)을 실행 증거로 확인
- 대조 로컬 규범: "Other tests still pass"는 테스트 스위트 전제. 로컬 회귀 확인 관례: 기존 정상 사례 재실행(M-0026 "정상=pdf_text 유지 확인"). self-test의 정본 트리거 문언은 "harness/ 데이터·문서를 변경한 경우"(CLAUDE.md·memory-harness 절차 1-2·관리_절차 4절 공통)이며 코드 변경은 그 문언 밖. 로컬 도구는 정상 동작에서도 stderr 진행 로그를 냄(extract-documents: "Progress/errors go to stderr")
- 판정 축: A(스위트 전제) + E(로컬 self-test 접속)
- 처리안: 대체 + 추가
- 수단: "재현 사례 통과 + 기존 정상 사례 회귀 재실행 + 출력에 예상 밖 오류·경고 없음(정상 진행 로그는 제외) + harness/ 변경이 데이터·문서에 닿으면 validate_harness.py exit 0(정본 요구), 코드만 변경해도 문서 기술과 어긋날 수 있으면 보수적으로 실행(정본을 넘어서는 확장임을 명시)"으로 구성. "Fix code, not test"의 의도(검증 기준을 산출에 맞추지 않는다) 보존
- 근거: 회귀 확인의 의도를 로컬 수단으로 달성. self-test 접속은 실증 근거(M-0018·M-0026이 실제 이 흐름) 있는 보수적 추가. (교정: pristine을 로컬 stderr 관례에 맞게 조건화, self-test 트리거를 정본 문언과 확장분으로 구별. 독립 검증 1-4·2-2)

### T-12 [반증 반영: 근거 조건화]
- 원본 위치: REFACTOR
- 지시 원문: "After green only: Remove duplication / Improve names / Extract helpers. Keep tests green. Don't add behavior."
- 유형: 명령(순서 규칙)
- 원본 의도: 정리 작업을 검증 통과 상태에서만 수행해 정리가 동작을 바꾸는 사고 방지
- 대조 로컬 규범: T-24 전제 하 저촉 없음(정리 대상이 harness/scripts면 불변 자산 보호의 사용자 확인이 선행되어야 하며, 그 게이트는 T-24가 커버)
- 판정 축: D(표층)
- 처리안: 대체(변환)
- 수단: 한국어 변환("green 유지"를 "재현·회귀 사례 통과 상태 유지"로)
- 근거: 순서 규칙은 도구 중립. (교정: "저촉 없음"을 T-24 전제부로 정밀화. 독립 검증 1-5)

### T-13 [반증 반영: 의도 보완]
- 원본 위치: Good Tests 표
- 지시 원문: "Minimal: One thing. 'and' in name? Split it. / Clear: Name describes behavior / Shows intent: Demonstrates desired API"
- 유형: 기본값(품질 기준)
- 원본 의도: 재현 사례의 가독성·귀속 명확성 + 원하는 동작(API)을 먼저 설계하게 유도(T-19 "Write wished-for API"와 연결)
- 대조 로컬 규범: 저촉 없음
- 판정 축: D(표층)
- 처리안: 대체(변환, "재현 사례" 어휘로)
- 근거: 무충돌 기준의 언어 변환. (교정: Shows intent의 제3 기능을 의도에 추가. 독립 검증 의도 표본)

### T-14 [반증 반영: 축 재판정 D→A 혼재]
- 원본 위치: Why Order Matters (5개 반박 논증)
- 지시 원문: "'I'll write tests after to verify it works' / Tests written after code pass immediately. Passing immediately proves nothing" 외 4건(수동 테스트 반박, 매몰비용 반박, dogmatic 반박, spirit-not-ritual 반박)
- 유형: 톤(합리화 반박 자산) + 설명
- 원본 의도: 스킵 합리화가 나타나는 문구를 미리 명명하고 반박해 둠(사전 봉쇄)
- 대조 로컬 규범: L-0005 값싼 디폴트로의 붕괴(교정 3형태 중 "작동 지점 직격"과 동일 기법). 단 반박 2("can't re-run when code changes / run the same way every time")와 반박 4("prevents regressions / enables refactoring")는 상주 자동 스위트의 존재를 논거로 사용, 로컬 부재 전제
- 판정 축: A 혼재(반박 논지 일부가 스위트 전제) + D(언어)
- 처리안: 대체(논지 재구성)
- 수단: 반박의 골격(합리화 명명 + 선반박)은 보존하되, 스위트 전제 논거는 로컬 형태로 재구성: "기록 없음·재실행 불가" 반박은 "재현 입력의 기록·보존"(커밋·M 노드에 재현 입력 남김)이 로컬에서 달성 가능한 형태로, 상주 회귀망 논거는 "다음 수정 때 기록된 재현 사례를 재실행"으로 격하해 정직하게 서술. 로컬 특유 합리화("스위트가 없으니 TDD 불가", "테스트 인프라 생기면 그때") 반박 추가
- 근거: 표층 변환만 하면 커스텀본이 자기 방법론(일회성 재현 실행)을 스스로 반박하는 자기모순 텍스트가 됨. (재판정: 독립 검증 1-1. 당초 D 단독 판정은 반박 논증 내부의 A 전제를 놓침)

### T-15 [반증 반영: 축 재판정·반복 구조 존치]
- 원본 위치: Common Rationalizations 표(11행)
- 지시 원문: "'Too simple to test' → Simple code breaks. Test takes 30 seconds." 외 10건. "Existing code has no tests → You're improving it. Add tests for existing code." 포함
- 유형: 톤(합리화 반박 표)
- 원본 의도: T-14와 동일 의도의 3중 봉쇄 구조 중 하나(본문 논증 / 요약 표 / Red Flags가 서로 다른 각도에서 같은 합리화를 차단). 반복 자체가 의도적 설계
- 대조 로컬 규범: T-14와 동일. "Add tests for existing code" 행은 로컬에 부재한 인프라의 신설 처방이라 변환 불가
- 판정 축: A 혼재 + D
- 처리안: 대체(논지 재구성, 3중 구조 존치)
- 수단: 본문 논증·표·Red Flags의 3중 구조는 유지(각각 논증/빠른 대조/자가 신호로 역할이 다름). 표 행별로 로컬 재구성: 스위트 전제 행은 재현 사례 어휘로 재구성하거나 로컬 대응물로 교체("기존 코드에 테스트 없음" 행은 "이 프로젝트엔 스위트가 없으니 불가" 합리화의 반박으로 재구성: 스위트가 없어도 재현 실행은 항상 가능). 합리화 트리거 문구가 전부 생존하는지 생성 시 점검
- 근거: 3중 반복은 T-03의 절대성과 같은 의도적 설계이므로 로컬 작문 규칙(같은 말 반복 금지)을 이유로 해체하지 않는다(역할이 다른 반복은 중복이 아님). (재판정: 독립 검증 1-1·2-5. 당초 "중복 일원화" 처리를 축소)

### T-16
- 원본 위치: Red Flags
- 지시 원문: "Code before test / Test after implementation / ... / 'This is different because...'" + "All of these mean: Delete code. Start over with TDD."
- 유형: 명령(중단 신호 목록)
- 원본 의도: 위반의 자기 감지 신호를 열거해 진행 중 자가 중단을 유도
- 대조 로컬 규범: 저촉 없음(L-0005 편향 자가진단 한계와 상보적: 신호 목록은 자가진단을 돕는 장치)
- 판정 축: D
- 처리안: 대체(변환)
- 수단: 한국어 변환 + 로컬 변형 신호("나중에 테스트 인프라 생기면 그때" 등) 추가
- 근거: 신호 목록은 도구 중립, 로컬 맥락의 신호만 보강

### T-17
- 원본 위치: Example: Bug Fix
- 지시 원문: typescript submitForm + npm test 트랜스크립트 예시 전체
- 유형: 예시(모범 절차 시연)
- 원본 의도: 추상 규칙을 구체 절차로 시연
- 대조 로컬 규범: 로컬 코드 실태(python 스크립트, 실데이터)
- 판정 축: A(언어·도구)
- 처리안: 대체
- 수단: 로컬 실증 사례(M-0026 extract_all.py 벡터 PDF 개선: 재현 입력 확정 → 실패 관찰 → 최소 수정 → 재현 통과 + 정상 회귀 확인)를 예시로 재작성
- 근거: 실제 있었던 로컬 사례가 가상 예시보다 시연 가치가 높고 검증 가능

### T-18 [반증 반영: 누락 2항목 승계·pristine 조건화]
- 원본 위치: Verification Checklist
- 지시 원문: "Every new function/method has a test / Watched each test fail before implementing / Each test failed for expected reason / Wrote minimal code to pass / All tests pass / Output pristine / Tests use real code (mocks only if unavoidable) / Edge cases and errors covered" + "Can't check all boxes? You skipped TDD. Start over."
- 유형: 명령(완료 게이트 체크리스트)
- 원본 의도: 완료 선언 전 위반의 사후 감지(체크 불가면 절차 위반이 있었다는 것)
- 대조 로컬 규범: 검증_체크리스트.md(문서용 완료 게이트)와 구조 동형이나 관할이 다름(코드 vs 문서)
- 판정 축: A(테스트 어휘)
- 처리안: 대체
- 수단: 재현 사례 어휘로 8항목 전부 승계: 모든 수정에 재현 사례 존재 / 실패를 직접 관찰 / 실패 이유가 예상과 일치 / 최소 수정 / 재현 통과 + 기존 사례 회귀 확인 / 출력에 예상 밖 오류·경고 없음(정상 진행 로그 제외) / 재현 입력이 실물 우선(대역은 불가피할 때만) / 경계 사례·오류 경로 포함
- 근거: 완료 게이트 구조는 로컬 문서 검증과 동형. (교정: 당초 6항목 변환에서 "실물 우선"·"경계 사례" 2항목 누락을 발견해 승계, pristine을 stderr 관례에 조건화. 독립 검증 3-1·1-4)

### T-19
- 원본 위치: When Stuck 표
- 지시 원문: "Don't know how to test → Write wished-for API. Write assertion first. Ask your human partner. / Test too complicated → Design too complicated. Simplify interface. / Must mock everything → Code too coupled. Use dependency injection. / Test setup huge → Extract helpers. Still complex? Simplify design."
- 유형: 명령(막힘 처방)
- 원본 의도: 검증이 어렵다는 신호를 설계 결함 신호로 역이용
- 대조 로컬 규범: 저촉 없음
- 판정 축: D(언어. "dependency injection" 등 어휘는 로컬 스크립트 규모에 과함)
- 처리안: 대체(변환·경량화)
- 수단: 한국어 변환, 로컬 규모에 맞는 처방(재현 사례를 못 만들겠으면 원하는 동작을 먼저 한 줄로 적기, 재현 준비가 크면 입력을 줄이기 등)
- 근거: "검증 곤란은 설계 결함 신호"의 의도 보존, 처방 어휘만 로컬화

### T-20 [반증 반영: 의도 보완]
- 원본 위치: Debugging Integration
- 지시 원문: "Bug found? Write failing test reproducing it. Follow TDD cycle. Test proves fix and prevents regression. Never fix bugs without a test."
- 유형: 명령
- 원본 의도: 버그 수정의 선행 조건으로 재현을 강제(재현 없는 수정은 수정 여부를 알 수 없음) + 보존된 테스트가 회귀를 방지(이중 기능)
- 대조 로컬 규범: M-0020(OCR "복원 불가" 단정이 재현·실측으로 뒤집힘), L-0005 표상 드리프트(진단도 정본 대조)
- 판정 축: A(테스트 어휘)
- 처리안: 대체
- 수단: "버그는 재현 입력을 먼저 확보하고 실패를 관찰한 뒤 수정한다. 재현 없이 수정하지 않는다"로 변환. 회귀 방지는 재현 사례의 기록·보존(커밋 메시지·M 노드에 재현 입력 기록 등)으로 승계하되, 상주 스위트의 자동 회귀망 대비 한계를 정직하게 명시
- 근거: 원본의 "테스트가 회귀를 막는다"는 스위트 상주를 전제하나, 로컬은 스위트가 없으므로 재현 사례의 기록·보존으로 의도 부분 달성. (교정: 의도 필드에 회귀 방지의 이중 기능 명시. 독립 검증 의도 표본)

### T-21 [반증 반영: 흡수 원리 2→4 확장, 하위 단위 명시]
- 원본 위치: Testing Anti-Patterns 절 + testing-anti-patterns.md 전체
- 지시 원문(하위 단위 분해): (a) 트리거 "Load this reference when: writing or changing tests, adding mocks, or tempted to add test-only methods" (b) Iron Laws 3: "NEVER test mock behavior / NEVER add test-only methods to production classes / NEVER mock without understanding dependencies" (c) AP1~AP5 각각의 위반·수정·Gate Function(의사코드 4개) (d) "When Mocks Become Too Complex" 경고 신호 (e) Quick Reference 표 6행 (f) Red Flags 6개 (g) "Mock the COMPLETE data structure as it exists in reality"(AP4 Iron Rule)
- 유형: 명령(참조 지시 + 리소스 규칙군)
- 원본 의도: 대역(mock)이 낳는 4갈래 자기기만의 차단: (1) 대역을 검증하며 실물을 검증했다고 믿음(AP1), (2) 검증 편의 코드가 실사용 코드를 오염(AP2), (3) 의존 구조를 이해하기 전에 대역으로 덮음(AP3), (4) 불완전한 대역이 실물 구조 가정을 숨김(AP4). AP5(테스트 사후 추가)는 Iron Law의 재확인
- 대조 로컬 규범: 로컬 코드에 테스트 프레임워크·mock 라이브러리 부재(실측). 로컬 검증 관례는 실데이터 직접 실행. AP3의 "불확실하면 실물 먼저 실행 후 관찰"은 M-0026(코드 정독·분포 진단 선행)과 정확히 동방향. AP4의 완전성 요구는 로컬 재현 입력 축소 제작 시 그대로 적용(extract_all.py 페이지 분기가 실물 구조 특성 의존)
- 판정 축: A(프레임워크 전제. 단 규칙의 상당수는 중립)
- 처리안: 생략(리소스 파일) + 대체(핵심 원리 4개 본문 흡수)
- 수단: testing-anti-patterns.md는 커스텀본에 미포함. 프레임워크 중립 원리 4개를 본문 1절로 흡수: (1) 대역(mock·축소 표본·가짜 입력)을 검증하고 실물을 검증했다고 믿지 않는다(실물 입력 우선), (2) 검증 편의를 위한 코드를 실사용 코드에 심지 않는다, (3) 무엇이 필요한지 불확실하면 실물로 먼저 실행해 관찰한 뒤 최소한만 대역으로 바꾼다, (4) 재현 입력을 축소 제작할 때 실물의 구조 특성을 보존한다(축소가 분기·가정을 바꾸면 재현이 아니다). mock 프레임워크 도입 시 리소스 재검토 트리거 명시
- 근거: 예시 코드는 jest/vi 특정이나 규칙 골격은 중립. 파일 생략은 유지하되 압축 손실(AP3·AP4)을 복구. (교정: 당초 "8할이 mock API 세부" 근거는 실측 과장이라 철회, 2원리를 4원리로 확장, 하위 단위를 지시 원문에 분해 기재. 독립 검증 2-1·3-2, 의도 표본)

### T-22
- 원본 위치: Final Rule
- 지시 원문: "Production code → test exists and failed first / Otherwise → not TDD" + "No exceptions without your human partner's permission."
- 유형: 명령(요약 규칙 + 예외 경로)
- 원본 의도: 규칙의 이진 판정화(해석 여지 제거) + 예외의 사용자 승인 일원화
- 대조 로컬 규범: T-05·T-06과 동일
- 판정 축: D
- 처리안: 대체(변환)
- 수단: "수정 코드에 선행 실패 재현이 있으면 이 절차를 지킨 것, 없으면 아닌 것. 예외는 사용자 승인만"으로 변환
- 근거: 이진 판정의 명료성 보존

### T-23 [반증 반영: 근거 완화]
- 원본 위치: Red-Green-Refactor dot 다이어그램
- 지시 원문: digraph tdd_cycle (RED→verify→GREEN→verify→REFACTOR 순환)
- 유형: 전제(형식 요소)
- 원본 의도: 사이클의 시각 요약
- 대조 로컬 규범: 강제 규범 없음. mermaid가 로컬에서 통용되는 표기이나(legal-harness 6단계의 산출물 형식) 스킬 문서 표기 규범은 아님
- 판정 축: D(경미)
- 처리안: 대체
- 수단: mermaid로 재작성(노드 라벨 어휘를 재현·수정·회귀 확인으로 변환. 어휘 변환은 T-06 계통상 필수이므로 재작성 자체는 필요)
- 근거: 시각 요약 가치는 유지. 표기 전환의 근거는 통용성 수준이며 어휘 변환이 재작성의 실질 이유. (교정: mermaid 근거의 과대 표기 완화. 독립 검증 2-4)

### T-24 [반증 반영: 문언 정밀화·라벨 해명]
- 원본 위치: (원본에 대응 절 없음. 역방향 점검에서 발견된 로컬 접촉)
- 지시 원문: 해당 없음. 로컬 규범: CLAUDE.md "불변 자산 보호: harness/ 디렉토리의 파일을 직접 수정하지 않는다. 수정이 필요하면 사용자에게 확인한다" / 관리_절차 4절(트리거 문언은 "하네스 데이터·문서 변경")
- 유형: (로컬 전제)
- 원본 의도: 해당 없음(로컬 의도: 하네스 정본의 무단 변경 방지)
- 대조 로컬 규범: advisory CLAUDE.md + 관리_절차.md 4절
- 판정 축: B(원본 Iron Law·REFACTOR가 harness/scripts에 발화하면 사용자 확인 없는 수정·삭제를 지시하는 격)
- 처리안: 추가(B 접촉의 해소 방식이 지시 대체가 아니라 로컬 선행 게이트의 명시이므로 처리 어휘는 추가)
- 수단: 커스텀본 적용 범위 절에 명시: 이 프로젝트 코드의 다수가 harness/scripts/에 있으므로 수정 착수 전 사용자 확인(CLAUDE.md 불변 자산 보호)이 선행하고, 변경 후 self-test는 정본 문언상 데이터·문서 변경이 트리거이되 코드 변경도 문서 기술과 어긋날 수 있어 보수적으로 실행(확장분임을 표기)
- 근거: 역방향 점검이 잡은 필수 접촉. 이 없이는 커스텀본이 로컬 보호 규범을 우회하는 절차서가 됨. (교정: self-test 트리거의 정본 문언 구별. 독립 검증 2-2·2-3)

## 절 귀속 확인 (전수성 점검)

SKILL.md: frontmatter(T-01) / Overview(T-02·03) / When to Use(T-04·05) / The Iron Law(T-06) / Red-Green-Refactor 도입·다이어그램(T-23) / RED(T-07) / Verify RED(T-08·09) / GREEN(T-10) / Verify GREEN(T-11) / REFACTOR(T-12) / Repeat(T-02에 승계 명시) / Good Tests(T-13) / Why Order Matters(T-14) / Common Rationalizations(T-15) / Red Flags(T-16) / Example(T-17) / Verification Checklist(T-18) / When Stuck(T-19) / Debugging Integration(T-20) / Testing Anti-Patterns(T-21) / Final Rule(T-22). 전 절 귀속 완료.
testing-anti-patterns.md: T-21에 하위 단위 (a)~(g)로 분해 귀속(독립 검증 3-2 반영).

## 붕괴 방지 점검 기록

1. 전수성: 상기 절 귀속 확인 완료(Repeat 절 승계를 T-02에 명시, anti-patterns 하위 단위 분해).
2. 무충돌 근거: F 단독 판정 행 없음. 실질 무충돌 행(T-10·12·13·16·19)에도 대조 근거 기재.
3. 역방향 점검: 기계 강제층(hook: `.claude/`·`harness/` 경로 게이트 통과, cases/ 문서는 커스텀본이 관할 배제로 접촉 해소, PreCompact는 법률 문서 전제라 무접촉, permissions는 python 직접 실행과 정합) / legal-harness(관할 경계 T-01·T-06) / memory-harness(커밋 지시가 원본에 없어 무접촉, 커밋 언급 추가는 억지 최적화로 미채택) / CLAUDE.md 불변 자산 보호(T-24) / 서브에이전트 제한(원본 무지시, 무접촉) / 열거 개방성(T-04) / 작문 규칙(커스텀본 본문에 적용, T-03 교정 반영).
4. 암묵 전제 스캔: 언어(영어→한국어, 전면 D) / 산출물 형태(코드 전제→조건화) / 실행 환경(npm→python 직접 실행) / 워크플로(git 지시 없음: "before commit"은 서술) / 서브에이전트(무지시) / 톤("your human partner"→사용자).
- 판정 분포: A 주축 12(T-01·02·04·06·07·08·11·14·15·17·18·20·21 중), B 1(T-24), C 요소 2(T-01·06), D 주축 9, E 1(T-11), F 단독 0. 전 행 무충돌 아님으로 분포 점검 통과.
- 계통 연쇄: T-06 대체(재현 실행)에 연동한 T-02·07·08·09·11·14·15·17·18·20·22의 어휘 일관("재현 사례") 확인. T-21 생략+4원리 흡수에 연동해 본문 참조 지시가 흡수 절로 대체. 고아 행 없음.

## 판정 분포 요약

- 총 24행(원본 유래 23 + 역방향 발견 1)
- 처리안: 대체 17(이 중 논지 재구성 2: T-14·15) / 조건화 1(T-04) / 생략+흡수 1(T-21) / 추가 2(T-11의 E 요소, T-24) / 유지(원형) 0 / 개정제안 0
- 권고: **커스텀 생성(①)**. 전제 불일치(테스트 인프라 부재)와 관할 경계(산문 정본 존재)가 스킬 골격에 미치므로 부분 조정으로는 부족
